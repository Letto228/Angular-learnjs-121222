import { ToFilterElementsPipe } from './to-filter-elements.pipe';

describe('ToFilterElementsPipe', () => {
	it('create an instance', () => {
		const pipe = new ToFilterElementsPipe();
		expect(pipe).toBeTruthy();
	});
});
