import { InsertScrollDirective } from './insert-scroll.directive';

describe('InsertScrollDirective', () => {
	it('should create an instance', () => {
		const directive = new InsertScrollDirective();
		expect(directive).toBeTruthy();
	});
});
