import { InfinityScrollDirective } from './infinity-scroll.directive';

describe('InfinityScrollDirective', () => {
	it('should create an instance', () => {
		const directive = new InfinityScrollDirective();
		expect(directive).toBeTruthy();
	});
});
