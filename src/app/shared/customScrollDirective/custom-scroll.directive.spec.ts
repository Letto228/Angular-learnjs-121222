import { appCustomInfinityScroll } from '../customScrollDirective/custom-scroll.directive';

describe('appCustomInfinityScroll', () => {
	it('should create an instance', () => {
		const directive = new appCustomInfinityScroll();
		expect(directive).toBeTruthy();
	});
});
