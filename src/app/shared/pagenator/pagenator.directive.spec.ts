import { PagenatorDirective } from './pagenator.directive';

describe('PagenatorDirective', () => {
  it('should create an instance', () => {
    const directive = new PagenatorDirective();
    expect(directive).toBeTruthy();
  });
});
